export const meta = {
  title: 'New year setup',
  description: 'Portfolio rebuild and content workflow.',
  date: '2026-01-01',
}

Rebuilt the personal site as a **Vite + React** app with MDX-backed learning notes and logs. Goal: add content by dropping files into `content/` without touching app code.

## Done

- Sidebar layout with learning discovery from MDX
- Calendar view grouped by log date
